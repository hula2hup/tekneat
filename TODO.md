# TODO: Hapus Fitur Upload Peta

## Langkah-langkah:
1. Hapus route `/upload_peta` di `app.py`. ✅
2. Hapus form upload peta dan link "Upload Peta" di `templates/admin.html`. ✅
3. Verifikasi bahwa gambar peta yang sudah ada tetap ditampilkan di `templates/peta.html`.
4. Kolom database `image_peta`, `image_peta2`, `image_peta3` tetap dipertahankan untuk menampilkan gambar yang sudah diupload.
